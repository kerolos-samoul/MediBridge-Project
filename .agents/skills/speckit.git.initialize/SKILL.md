---
name: speckit.git.initialize
description: Use when the user invokes the Spec Kit git initialization helper or when a Spec Kit workflow hook requests speckit.git.initialize.
---

# Spec Kit Git Initialize

Read `.github/agents/speckit.git.initialize.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
