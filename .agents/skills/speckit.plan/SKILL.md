---
name: speckit.plan
description: Use when the user invokes Spec Kit plan workflow to generate implementation plan artifacts from the current feature specification.
---

# Spec Kit Plan

Read `.github/agents/speckit.plan.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
