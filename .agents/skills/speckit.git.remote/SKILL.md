---
name: speckit.git.remote
description: Use when the user invokes the Spec Kit git remote helper or when a Spec Kit workflow hook requests speckit.git.remote.
---

# Spec Kit Git Remote

Read `.github/agents/speckit.git.remote.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
