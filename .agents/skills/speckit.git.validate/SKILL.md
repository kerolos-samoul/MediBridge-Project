---
name: speckit.git.validate
description: Use when the user invokes the Spec Kit git validation helper or when a Spec Kit workflow hook requests speckit.git.validate.
---

# Spec Kit Git Validate

Read `.github/agents/speckit.git.validate.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
