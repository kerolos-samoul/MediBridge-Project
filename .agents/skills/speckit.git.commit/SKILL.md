---
name: speckit.git.commit
description: Use when the user invokes the Spec Kit git commit helper or when a Spec Kit workflow hook requests speckit.git.commit.
---

# Spec Kit Git Commit

Read `.github/agents/speckit.git.commit.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
