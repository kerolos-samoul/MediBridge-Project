---
name: speckit.specify
description: Use when the user invokes Spec Kit specify workflow to create or update a feature specification from a natural language feature description.
---

# Spec Kit Specify

Read `.github/agents/speckit.specify.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
