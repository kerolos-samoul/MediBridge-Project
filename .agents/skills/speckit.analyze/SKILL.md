---
name: speckit.analyze
description: Use when the user invokes Spec Kit analyze workflow to check consistency across spec.md, plan.md, and tasks.md.
---

# Spec Kit Analyze

Read `.github/agents/speckit.analyze.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
