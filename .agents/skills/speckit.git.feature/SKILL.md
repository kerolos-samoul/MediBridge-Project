---
name: speckit.git.feature
description: Use when the user invokes the Spec Kit git feature helper or when a Spec Kit workflow hook requests speckit.git.feature.
---

# Spec Kit Git Feature

Read `.github/agents/speckit.git.feature.agent.md` from the repository root and follow it exactly.

Treat the user's text after the invocation as `$ARGUMENTS`.
